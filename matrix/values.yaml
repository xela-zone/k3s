# Element Server Suite (ESS) Official Values v26.2.0
serverName: "kbrt.xyz"

# Synapse
synapse:
  ingress:
    host: "matrix.kbrt.xyz"
  postgres:
    host: "matrix-db-rw.matrix.svc"
    port: 5432
    user: "synapse"
    database: "synapse"
    sslMode: "prefer"
    password:
      value: "PLACEHOLDER"

# Matrix Authentication Service (MAS)
matrixAuthenticationService:
  ingress:
    host: "matrix-auth.kbrt.xyz"
  postgres:
    host: "matrix-db-rw.matrix.svc"
    port: 5432
    user: "synapse"
    database: "synapse"
    sslMode: "prefer"
    password:
      value: "PLACEHOLDER"

# Element Web
elementWeb:
  ingress:
    host: "chat.kbrt.xyz"

# Ingress Global Config
ingress:
  # ingressClassName is not allowed, so we leave it empty and hope for default
  # or use annotations if allowed.
  {}